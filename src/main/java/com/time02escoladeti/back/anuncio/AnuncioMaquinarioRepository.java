package com.time02escoladeti.back.anuncio;

import org.springframework.data.jpa.repository.JpaRepository;

public interface AnuncioMaquinarioRepository extends JpaRepository<AnuncioMaquinario, AnuncioId> {
}
